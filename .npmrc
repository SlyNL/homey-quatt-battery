; NPM configuration for consistent builds
; Ensures reproducible installs across all environments

; Use exact versions for reproducibility
save-exact=true

; Use the latest version of npm
; This helps avoid compatibility issues
engine-strict=true

; Registry configuration
registry=https://registry.npmjs.org/

; Prevent accidental publishing
; Remove this line if you plan to publish to npm
access=restricted
